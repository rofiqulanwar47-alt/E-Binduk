import { Student } from '../types';

/**
 * Default initial students array.
 * Kept empty so only real imported students (e.g. 384 students from Excel)
 * are populated and managed in the system.
 */
export const initialStudents: Student[] = [];
